# Byte-compiled output is per-Emacs-version and gets regenerated by
# config.org's `my/-load-module' on first launch. Never commit it.
*.elc

# Org-mode tangle artifacts and editor backups
*~
\#*\#
.\#*

# Claude Code workspace state (per-user, not part of the distro)
.claude/

# macOS metadata
.DS_Store

# install.sh's BSD-sed temp files (sweep them up if any leak)
.\!*\!*
