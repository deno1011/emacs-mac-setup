#+TITLE: Index — Emacs Knowledge Wiki
#+STARTUP: overview

Catalog of all wiki pages. The agent reads this first when answering
queries. Regenerate from disk whenever pages are added/removed (or let
the ingest workflow update it on each new ingest).

* Sources

* Entities

* Concepts

* Comparisons

* Overviews
